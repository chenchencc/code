1.Scala混合类型 Any、Some、AnyVal、

Unit == Java中的void类型

1 to 10
1. to 10
1 until 10(1,2,3,4,5,6,7,8,9)不到10

声明一个数组：
var array = Array("a","b")

<-:表示遍历一个数组的操作
for(i <- array){
println(i)
}

// 重命名成员
import java.util.{HashMap => JavaHashMap}

// 隐藏成员
import java.util.{HashMap => _, _} // 引入了util包的所有成员，但是HashMap被隐藏了

yield:scala中的yield关键字是将每一个值放入到一个集合中，

for(i <- 1 t0 10) yield i

def add(i:Int,y:Int):Int={
x+y
}

var m = (i:Int,y:Int) => x+y
